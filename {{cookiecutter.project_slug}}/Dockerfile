{% if cookiecutter.include_cdk == 'yes' %}# Dockerfile not needed for Lambda deployment
# Lambda functions are deployed as zip packages via CDK
# This file is kept as placeholder for potential container-based deployments
{% endif %}